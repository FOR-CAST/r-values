## NOTE: several suggested packages used here indirectly;
## library() calls included here to appease renv.
if (FALSE) {
  library(BioSIM)
  library(cluster)
  library(foreign)
  library(httpuv)
  library(J4R)
  library(later)
  library(promises)
  library(rpart)
  library(spatial)
  library(XML)
}
